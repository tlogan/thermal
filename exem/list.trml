with + infixl d0 : add
with - infixl d0 : sub
with * infixl d1 : mul
with / infixl d1 : div

with length : (
  case (# x xs) => (with (log `ooga`) (1 + (length xs)))
  case () => (with (log `ooga`) 0) //blank matches everything, so must be placed last
)

with (log (#1 #2 #3))

with (log (length (# 1 # 2 # 3)))

()
