make_nat_stream : () => (
  ch : alloc_chan ();
  count : i => (
    sync (send (ch, i,));
    count (add (i, 1,))
  );
  spawn (() => count 0); 
  ch
);
()
/*

nstream : make_nat_stream ();
loop : (
  case 0 => () 
  case n =>
    sync (recv nstream);
    loop (sub (n, 1,))
);
loop 9
*/